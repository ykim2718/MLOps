# docker-compose.env_example  (placeholders for the study stack — never put real secrets here; it is committed to git. PGADMIN_DEFAULT_EMAIL must be a valid email format.)
POSTGRES_USER=CHANGE_ME
POSTGRES_PASSWORD=CHANGE_ME
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=CHANGE_ME
