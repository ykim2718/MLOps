# docker-compose.env_example  (MongoDB)
# 모든 CHANGE_ME 를 실제 값으로 채운다. 이 파일은 비밀값을 담으므로 .gitignore 로 git 추적에서 제외한다.

# MongoDB 루트 계정. 컨테이너 최초 기동 시 이 계정(admin DB)으로 인증이 켜진다.
# 같은 값을 이 DB 에 접속하는 서비스/코드 쪽 연결 문자열에도 맞춰 적는다.
MONGO_INITDB_ROOT_USERNAME=CHANGE_ME
MONGO_INITDB_ROOT_PASSWORD=CHANGE_ME
